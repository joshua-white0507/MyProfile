<h1>All Entries</h1>
<div class='container-fluid'>
 <% @users.each do |user| %>
   <%= user.name %>
   <%= user.practice %>
  <%= button_to "Delete", user, method: :delete, data: { confirm: "Are you sure you want to delete this Project ?"} %>
  <% end %>
 
</div>